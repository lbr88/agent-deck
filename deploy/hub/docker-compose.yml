services:
  agent-deck-hub:
    build:
      context: ../..
      dockerfile: deploy/hub/Dockerfile
    image: agent-deck-hub:local
    command:
      - --listen=:8421
      - --data=/data
    ports:
      - "8421:8421"
    volumes:
      - ./data:/data
    restart: unless-stopped
